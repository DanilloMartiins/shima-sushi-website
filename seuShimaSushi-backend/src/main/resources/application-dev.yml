spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:SeuShimaSushi}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Ester@2020}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    validate-on-migrate: false
    locations: classpath:db/migration
    placeholders:
      admin_name: ${ADMIN_NAME:Seu Shima Sushi Admin}
      admin_email: ${ADMIN_EMAIL:admin@seushimasushi.com}
      admin_password: ${ADMIN_PASSWORD:Admin@123}
      admin_clerk_id: ${ADMIN_CLERK_ID:placeholder-clerk-id}

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://informed-haddock-50.clerk.accounts.dev

app:
  security:
    jwt:
      secret: ${JWT_SECRET:4f7a2b9c8d1e3f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a}

logging:
  level:
    org.springframework.security: INFO